/**
 * Created by lxy on 16/8/17.
 */
import { createHistory } from 'history';
import { useRouterHistory } from 'react-router';
const history = useRouterHistory(createHistory)()

export default history;